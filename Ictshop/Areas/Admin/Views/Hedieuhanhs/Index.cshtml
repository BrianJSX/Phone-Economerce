@model IEnumerable<Ictshop.Models.Hedieuhanh>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="admin-content-main">
    <div class="admin-content-main-header">
        <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="#">Home</a></li>
                <li class="breadcrumb-item active" aria-current="page">Library</li>
            </ol>
        </nav>
        <button class="btn btn-success">@Html.ActionLink("Thêm mới hệ điều hành", "Create")</button>
    </div>
    <div class="admin-content-main-body">
        <table class="table table-bordered">
            <tr>
                <th>
                    Mã hệ điều hành
                </th>
                <th>
                    Tên hệ điều hành
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Mahdh)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Tenhdh)
                    </td>
                    <td>
                        @Html.ActionLink("Sửa", "Edit", new { id = item.Mahdh }) |
                        @Html.ActionLink("Chi tiết", "Details", new { id = item.Mahdh }) |
                        @Html.ActionLink("Xóa ", "Delete", new { id = item.Mahdh })
                    </td>
                </tr>
            }

        </table>
    </div>
</div>

